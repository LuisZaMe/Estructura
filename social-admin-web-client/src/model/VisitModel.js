class VisitModel {
    constructor(id, idClient, idCandidate, idInterviewer, visitDate, target, studyId, state, city, meetMode) {
        this.id = id 
        this.study = {stud}
        VisitDate 
        ConfirmAssistance 
        VisitStatus 
        City 
        State 
        Address 
        Observations 
        NotationColor 
    }
}

export default VisitModel
